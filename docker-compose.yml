services:
  app:
    build:
      context: .
    image: "${NVIDIA_ROUTER_IMAGE:-nvidia-router:local}"
    restart: unless-stopped
    init: true
    ports:
      - "127.0.0.1:3756:3756"
    environment:
      NVIDIA_ROUTER_MASTER_KEY: "${NVIDIA_ROUTER_MASTER_KEY:?NVIDIA_ROUTER_MASTER_KEY is required}"
      NVIDIA_ROUTER_INITIAL_ADMIN_PASSWORD: "${NVIDIA_ROUTER_INITIAL_ADMIN_PASSWORD:?NVIDIA_ROUTER_INITIAL_ADMIN_PASSWORD is required}"
      NVIDIA_ROUTER_ADMIN_SECURE_COOKIE: "${NVIDIA_ROUTER_ADMIN_SECURE_COOKIE:-false}"
      NVIDIA_ROUTER_ADMIN_EXTERNAL_ORIGIN: "${NVIDIA_ROUTER_ADMIN_EXTERNAL_ORIGIN:-}"
      NVIDIA_ROUTER_TRUSTED_PROXY_CIDRS: "${NVIDIA_ROUTER_TRUSTED_PROXY_CIDRS:-}"
      NVIDIA_ROUTER_LISTEN_ADDR: "${NVIDIA_ROUTER_LISTEN_ADDR:-0.0.0.0:3756}"
      NVIDIA_ROUTER_DATA_DIR: "${NVIDIA_ROUTER_DATA_DIR:-/data}"
      NVIDIA_ROUTER_TEMP_DIR: "${NVIDIA_ROUTER_TEMP_DIR:-/tmp}"
      NVIDIA_ROUTER_NVIDIA_BASE_URL: "${NVIDIA_ROUTER_NVIDIA_BASE_URL:-https://integrate.api.nvidia.com}"
      NVIDIA_ROUTER_XK_UPSTREAM_URL: "${NVIDIA_ROUTER_XK_UPSTREAM_URL:-}"
      NVIDIA_ROUTER_XK_VALIDATION_URL: "${NVIDIA_ROUTER_XK_VALIDATION_URL:-}"
      NVIDIA_ROUTER_XK_VALIDATION_STATUS: "${NVIDIA_ROUTER_XK_VALIDATION_STATUS:-404}"
      NVIDIA_ROUTER_XK_UPSTREAM_TIMEOUT: "${NVIDIA_ROUTER_XK_UPSTREAM_TIMEOUT:-4s}"
      NVIDIA_ROUTER_XK_VALIDATION_TIMEOUT: "${NVIDIA_ROUTER_XK_VALIDATION_TIMEOUT:-5s}"
      NVIDIA_ROUTER_XK_COLLECT_INTERVAL: "${NVIDIA_ROUTER_XK_COLLECT_INTERVAL:-5s}"
      NVIDIA_ROUTER_XK_PROXY_TTL: "${NVIDIA_ROUTER_XK_PROXY_TTL:-120s}"
      NVIDIA_ROUTER_XK_EXPECTED_QTY: "${NVIDIA_ROUTER_XK_EXPECTED_QTY:-2}"
      NVIDIA_ROUTER_XK_CONCURRENCY: "${NVIDIA_ROUTER_XK_CONCURRENCY:-2}"
    volumes:
      - nvidia-router-data:/data
    tmpfs:
      - /tmp:size=64m,mode=1777
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "--show-error", "--max-time", "3", "http://127.0.0.1:3756/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    stop_grace_period: 10m

volumes:
  nvidia-router-data:
