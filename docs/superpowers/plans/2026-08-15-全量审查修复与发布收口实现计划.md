# 全量审查修复与发布收口实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 `subagent-driven-development` 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将当前工作树的合法业务改动收口，并修复全量审查确认的安全、资源生命周期、数据迁移、部署和配置一致性问题，使修复分支与合入后的 `main` 通过完整 CI。

**架构：** 保留现有单体 NVIDIA 请求路径；在共享边界修复限流、请求 context、流式写入和 Collector 生命周期。Provider 只保留安全存储能力，服务端拒绝启用尚未接入运行时的非 NVIDIA Provider。数据库升级采用事务内复制保留，部署脚本从 Compose 的实际资源定义派生卷和镜像标签。

**技术栈：** Go 1.24、SQLite、标准库 `context`/`net/http`/`net/url`、Vue 3 + TypeScript、Vitest、pnpm、Docker Compose、GitHub Actions。

---

## 全局约束

- 当前分支是 `fix/audit-hardening-20260815`；不得在 `main` 上直接实现。
- 当前工作树已有用户改动；不回退、不覆盖、不使用 `git clean`、`reset --hard` 或 `git add .`。
- 只提交能证明属于产品源码、自动化测试、数据库迁移、嵌入资产或必要配置的文件。
- 凭据、真实运行数据、日志、WAL/SHM、二进制、缓存和一次性远程 root 脚本不提交，也不在报告中回显。
- 每个行为修复先写失败测试并确认失败，再写最少生产代码；配置文件修改可直接修改后验证。
- 任务编号使用 101–109，避免与 `.superpowers/sdd/progress.md` 中历史任务冲突。
- 每个任务只修改自己的写集；任务完成后运行覆盖测试、生成 diff 审查包并完成规格/质量审查，再进入下一任务。

## 文件清单与职责

### 认证、审计与请求边界

- 修改：`internal/adminauth/ratelimit.go`、`internal/httpapi/admin/auth.go`、对应测试
- 修改：`internal/httpapi/admin/audit.go`、`internal/adminaudit/recorder.go`、对应测试
- 修改：`internal/httpapi/v1/bodybudget.go`、`internal/httpapi/v1/bodybudget_test.go`

### 流式和连接生命周期

- 修改：`internal/router/attempt.go`、`internal/httpapi/v1/chat.go`、`internal/httpapi/v1/responses.go`、`internal/httpapi/v1/audio.go`
- 修改：`internal/sse/proxy.go`、`internal/httpapi/admin/event_stream.go`
- 测试：对应 `*_test.go`

### 代理采集、设置和迁移

- 修改：`internal/xkproxy/collector.go`、`internal/xkproxy/upstream.go`、`internal/xkproxy/settings.go`
- 修改：`internal/database/migrations/023_builtin_proxy_pool_safe_settings.sql`
- 创建：`internal/database/migrations/024_disable_unsupported_provider_credentials.sql`
- 测试：Collector、设置、上游和迁移测试

### Provider 安全行为

- 修改：`internal/httpapi/admin/provider_credentials.go`、`internal/providercredential/repository.go`
- 修改：`internal/modelcatalog/service.go` 或其共享校验位置及对应测试
- 修改：Provider 管理前端文案/禁用态（仅在现有组件可安全表达时）

### 前端和部署

- 修改：`web/src/features/proxy/types.ts`、`web/src/features/proxy/ProxyPoolView.vue`、对应 spec
- 修改：`.gitignore`、`.dockerignore`、`Dockerfile`、`docker-compose.yml`、部署/回滚/Litestream/CLI 文档和脚本
- 修改：`.github/workflows/ci.yml`，补充脚本语法检查

---

### 任务 101：整理并提交现有合法业务改动

**文件：**

- 修改：`.gitignore`、`.dockerignore`
- 暂存：现有产品源码、测试、前端嵌入资产和不含敏感数据的必要配置
- 排除：`data/`、`*.log`、`*.exe`、`web/test-results/`、`scripts/test/` 中的一次性 Python 远程脚本、`start.bat`、临时命名文件和运行报告

- [ ] **步骤 1：建立排除规则**

  只增加可验证的项目级规则，例如 `/data/`、`/web/test-results/`、`/scripts/test/__pycache__/`、本地启动文件和根目录运行日志；不使用宽泛规则隐藏产品源码。

- [ ] **步骤 2：运行基线检查**

  运行：`go test ./...`、`go vet ./...`、`pnpm --dir web run typecheck`、`pnpm --dir web run test`、`git diff --check`。

  预期：基线命令通过；任何已有失败必须记录并在本任务停止，不把失败归因于本轮修复。

- [ ] **步骤 3：建立显式暂存清单并提交**

  使用 `git add -- <明确路径>`，检查 `git diff --cached --name-status` 与 `git diff --cached --check`，确认 staged diff 不含密钥、真实数据和临时资产。

  提交：`chore: 收口现有产品改动与本地资产边界`。

---

### 任务 102：修复管理员登录 DoS 与审计取消

**文件：**

- 修改：`internal/adminauth/ratelimit.go`、`internal/httpapi/admin/auth.go`
- 修改：`internal/httpapi/admin/audit.go`、`internal/adminaudit/recorder.go`
- 测试：`internal/adminauth/ratelimit_test.go`、`internal/httpapi/admin/auth_test.go`、`internal/httpapi/admin/audit_test.go`

- [ ] **步骤 1：编写失败测试**

  增加以下行为断言：

  ```go
  func TestLoginLimiterIPBucketRejectsRotatedUsernames(t *testing.T)
  func TestLoginLimiterStateHasBoundedCardinality(t *testing.T)
  func TestAuthMalformedLoginConsumesIPAttempt(t *testing.T)
  func TestAuditRecordSurvivesRequestCancellation(t *testing.T)
  ```

  前两个测试使用现有 fake clock；第三个通过 handler 发送畸形 JSON 后连续请求不同用户名；第四个让 repository fake 在 `ctx.Done()` 已关闭时仍接收独立审计 context。

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/adminauth ./internal/httpapi/admin -run 'Test(LoginLimiter|AuthMalformed|AuditRecord)' -count=1`。

  预期：新测试因用户名轮换、未计入的 malformed body、或取消 context 而失败，而不是编译错误。

- [ ] **步骤 3：实现最少修复**

  `StartAttempt` 在 IP 级桶和账号/IP 级桶上都执行；先检查 IP 桶再进入 Argon2；所有 login 解析失败路径使用 IP 桶。为状态 map 增加固定上限和最久未使用淘汰，保留过期清理。审计写入改用保留 principal 的 `context.WithoutCancel`，再套独立短超时。

- [ ] **步骤 4：运行绿灯与回归**

  运行定向测试，再运行 `go test ./internal/adminauth ./internal/httpapi/admin ./internal/httpapi/v1 -count=1`。

- [ ] **步骤 5：提交**

  提交：`fix: 限制管理员认证资源消耗并保留审计`。

---

### 任务 103：修复流式响应和管理员 SSE 的下游阻塞

**文件：**

- 修改：`internal/router/attempt.go`、`internal/httpapi/v1/chat.go`、`internal/httpapi/v1/responses.go`、`internal/httpapi/v1/audio.go`
- 修改：`internal/sse/proxy.go`、`internal/httpapi/admin/event_stream.go`
- 测试：`internal/router/attempt_test.go`、`internal/sse/proxy_test.go`、`internal/httpapi/v1/*_test.go`、`internal/httpapi/admin/event_stream_test.go`

- [ ] **步骤 1：编写失败测试**

  增加：

  ```go
  func TestAttemptResultCarriesStreamingBudgetContext(t *testing.T)
  func TestFlushWithUnsupportedDeadlineDoesNotSpawnUnboundedFlush(t *testing.T)
  func TestResponsesEmitterStopsOnWriteDeadline(t *testing.T)
  func TestSpeechUsesAttemptContextForDownstreamWrite(t *testing.T)
  func TestEventStreamRejectsConnectionWhenSubscriberLimitReached(t *testing.T)
  ```

  使用可控的 `ResponseWriter`：支持 `SetWriteDeadline` 的 writer 在 deadline 后返回错误；不支持 deadline 的 writer 断言函数立即返回明确错误且没有遗留 goroutine。不要用固定 sleep 证明成功。

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/router ./internal/sse ./internal/httpapi/v1 ./internal/httpapi/admin -run 'Test(AttemptResult|Flush|ResponsesEmitter|Speech|EventStream)' -count=1`。

- [ ] **步骤 3：实现最少修复**

  在 `AttemptResult` 返回最终流式 context；Chat、Responses、Speech 使用该 context，不再重新取原始 request context。统一在写入和 flush 前设置 `http.ResponseController` deadline，unsupported 时返回可识别错误，不以无法取消的同步 flush 或裸 goroutine 作为 fallback。Responses emitter 也使用同一写保护。管理员 SSE 增加有界连接槽、写 deadline 和取消清理。

- [ ] **步骤 4：运行绿灯与并发检查**

  运行定向测试、`go test ./internal/router ./internal/sse ./internal/httpapi/v1 ./internal/httpapi/admin -count=1`，并在 Linux CI 依赖的实现不变前提下运行本机可用的 `go test -race` 子集。

- [ ] **步骤 5：提交**

  提交：`fix: 约束流式响应的下游资源生命周期`。

---

### 任务 104：修复 Collector 关闭竞态

**文件：**

- 修改：`internal/xkproxy/collector.go`
- 测试：`internal/xkproxy/collector_lifecycle_test.go`

- [ ] **步骤 1：编写失败测试**

  增加 `TestCollectorCloseWaitsForManualRefresh`：让 upstream 在请求开始后阻塞，启动 `Refresh`，确认 `Close` 在释放 upstream 前等待刷新退出；关闭后再调用 `Refresh` 必须立即返回 closed 错误。

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/xkproxy -run 'TestCollectorCloseWaitsForManualRefresh' -count=1`。

- [ ] **步骤 3：实现最少修复**

  把每次进入 `fetchSingleFlight` 的刷新注册到 Collector 生命周期；Close 设置关闭闸门、关闭 done/cancel，再等待 scheduler 和手工刷新全部结束后关闭依赖。避免在锁内等待网络请求。

- [ ] **步骤 4：运行绿灯与回归**

  运行 `go test ./internal/xkproxy -run 'TestCollector' -count=1` 和 `go test ./internal/xkproxy -count=1`。

- [ ] **步骤 5：提交**

  提交：`fix: 等待代理采集刷新完成后关闭`。

---

### 任务 105：把 023 改为保留式迁移，并修复请求体边界

**文件：**

- 修改：`internal/database/migrations/023_builtin_proxy_pool_safe_settings.sql`
- 修改：`internal/database/migration_023_test.go`
- 修改：`internal/httpapi/v1/bodybudget.go`
- 测试：`internal/httpapi/v1/bodybudget_test.go`

- [ ] **步骤 1：编写失败测试**

  将迁移测试改为插入旧表的 enabled、proxy URL、加密 nonce/ciphertext、version、key version 和时间后，断言新表仍能读取相同值；新增 chunked body 在第 `limit` 字节后仍有一个字节时返回 `request_too_large`。

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/database -run 'TestMigration023' -count=1` 与 `go test ./internal/httpapi/v1 -run 'Test.*Body' -count=1`。

- [ ] **步骤 3：实现最少修复**

  023 在事务内创建安全结构、从旧表复制可映射列并验证，再完成替换；不要在日志中输出加密内容。chunked reader 使用 `limit+1` 的探测读取，超过上限时释放 lease 并返回现有结构化错误。

- [ ] **步骤 4：运行绿灯与升级回归**

  运行迁移全量测试、bodybudget 全量测试和 `go test ./...`。

- [ ] **步骤 5：提交**

  提交：`fix: 保留代理配置并严格限制请求体大小`。

---

### 任务 106：关闭未接入运行时的 Provider，并修复 upsert/URL 校验

**文件：**

- 修改：`internal/httpapi/admin/provider_credentials.go`
- 修改：`internal/providercredential/repository.go`
- 修改：`internal/modelcatalog/service.go` 或共享模型校验位置
- 创建：`internal/database/migrations/024_disable_unsupported_provider_credentials.sql`
- 测试：Provider、modelcatalog 和 migration 测试

- [ ] **步骤 1：编写失败测试**

  增加：

  ```go
  func TestProviderCreateRejectsUnsafeBaseURL(t *testing.T)
  func TestProviderEnableRejectsUnsupportedRuntime(t *testing.T)
  func TestModelPatchRejectsOpenAICompatibleProvider(t *testing.T)
  func TestProviderUpsertReturnsConflictedRowID(t *testing.T)
  ```

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/httpapi/admin ./internal/providercredential ./internal/modelcatalog -run 'Test(Provider|Model.*Provider)' -count=1`。

- [ ] **步骤 3：实现最少修复**

  服务端只接受无 userinfo/query/fragment、带 host 的 HTTP(S) URL；`SetEnabled(true)` 和模型 provider 非 `nvidia` 的修改返回稳定的 400 错误。Provider 创建默认为 disabled。upsert 改用唯一键回查或 SQLite `RETURNING`。024 将已有未支持 Provider 置为 disabled。

- [ ] **步骤 4：运行绿灯与数据库回归**

  运行定向测试、迁移测试和 `go test ./internal/httpapi/admin ./internal/providercredential ./internal/modelcatalog ./internal/database -count=1`。

- [ ] **步骤 5：提交**

  提交：`fix: 阻止未接入运行时的 Provider 被启用`。

---

### 任务 107：修复代理池 PATCH、ExpectedQty 和前端回填

**文件：**

- 修改：`internal/xkproxy/settings.go`、`internal/xkproxy/upstream.go`
- 修改：`internal/httpapi/admin/proxy_pool.go`
- 修改：`web/src/features/proxy/types.ts`、`web/src/features/proxy/ProxyPoolView.vue`
- 测试：`internal/xkproxy/settings_test.go`、`internal/httpapi/admin/proxy_pool_test.go`、`web/src/features/proxy/ProxyPoolView.spec.ts`

- [ ] **步骤 1：编写失败测试**

  覆盖状态码越界、零/负并发、非法 ExpectedQty、TTL/周期错误关系、空 MaxLatency 清除；加载完整配置后直接保存不得改变数据库字段；上游请求必须携带配置中的数量值。

- [ ] **步骤 2：运行红灯测试**

  运行：`go test ./internal/xkproxy ./internal/httpapi/admin -run 'Test.*(Patch|Settings|ExpectedQty)' -count=1` 与 `pnpm --dir web exec vitest run src/features/proxy/ProxyPoolView.spec.ts`。

- [ ] **步骤 3：实现最少修复**

  PATCH 字段使用 nullable 语义区分省略和清除；服务端统一校验所有约束。Snapshot 返回 UI 保存所需的完整非敏感字段。UpstreamClient 在内存 URL 上设置 validated quantity；前端 `applySettings` 回填所有字段并以完整当前值提交，不回显上游密钥。

- [ ] **步骤 4：运行绿灯与前端回归**

  运行定向 Go/Vitest、`pnpm --dir web run typecheck`、`pnpm --dir web run lint`、`pnpm --dir web run test`。

- [ ] **步骤 5：提交**

  提交：`fix: 保持代理池配置与运行时一致`。

---

### 任务 108：统一部署、Litestream、CLI 和凭据边界

**文件：**

- 修改：`Dockerfile`、`.dockerignore`、`docker-compose.yml`
- 修改：`scripts/deploy/deploy-public.sh`、`scripts/deploy/rollback-public.sh`、`scripts/litestream-manage.sh`、`docker-compose.litestream.yml`
- 修改：`internal/app/app.go`、`internal/app/cli.go` 及对应测试、相关部署文档
- 创建：`internal/processlock/lock_unix.go`、`internal/processlock/lock_windows.go`、`internal/processlock/lock_test.go`
- 修改：`.github/workflows/ci.yml`
- 修改：`start.bat` 仅改为运行时环境变量模板；不提交该本地启动文件

- [ ] **步骤 1：编写脚本/CLI 回归测试**

  断言 CLI 只接受 `db backup --output <path>`，并为 Compose 中实际镜像标签、脚本语法、备份卷派生和进程锁互斥增加可重复自测；baseline 流程不得调用 restore。测试输出只允许打印资源名和状态，不打印环境变量值。

- [ ] **步骤 2：实现最少修复**

  Compose 暴露 `NVIDIA_ROUTER_IMAGE`，部署脚本设置该变量并使用 `--no-build`；备份/回滚从 Compose label 或容器 mount 派生唯一 data volume，移除吞错和不存在端口。Litestream baseline 改为启动/确认 replicate 流程，CLI 文档与脚本统一 `db backup --output <path>`。应用服务在数据库生命周期内持有 OS advisory lock，离线维护命令非阻塞获取同一锁，替代存在竞态的 `-shm` 文件大小判断。Unix/Windows 实现复用已安装的 `golang.org/x/sys`，不增加依赖。Dockerfile 只消费运行时 Secret，build context 排除本地敏感文件。

- [ ] **步骤 3：运行验证**

  运行：`bash -n scripts/deploy/deploy-public.sh scripts/deploy/rollback-public.sh scripts/litestream-manage.sh scripts/test/live-nvidia.sh scripts/test/compose-acceptance.sh scripts/check-secrets.sh`、CLI 定向测试、`scripts/check-secrets.sh`。

- [ ] **步骤 4：提交**

  提交：`fix: 统一部署备份与构建凭据边界`。

---

### 任务 109：分支集成、全量验证和发布

**文件：**

- 不新增业务文件；只生成任务报告和 Git 审查包到 `.superpowers/sdd/`（忽略目录）

- [ ] **步骤 1：本地全量验证**

  运行项目 CI 对应命令：前端 lint/typecheck/test/build，Go test/race/vet/golangci-lint（本机缺失时标记），Shell self-tests，secret scan，Docker build，Compose acceptance，E2E 和 `git diff --check`。

- [ ] **步骤 2： staged/工作树审计**

  检查 staged 文件清单、敏感模式和 Docker build context；确认未跟踪运行产物不会随提交进入远端。

- [ ] **步骤 3：最终代码审查**

  以 `git merge-base main HEAD` 为基线生成完整 diff，完成规格合规和安全/并发审查；Critical/Important 问题修复并重跑覆盖测试。

- [ ] **步骤 4：推送修复分支并等待 GitHub Actions**

  使用 `git push -u origin fix/audit-hardening-20260815`。读取 `verify` 和 `e2e` 的真实运行结果；失败时根据日志修复，不跳过检查。

- [ ] **步骤 5：合入 main 并复验**

  在所有分支检查通过后合入本地 `main`，推送 `origin/main`，再等待 main push 触发的完整 CI。记录最终 commit、Actions run 和未验证的真实代理联调项。

## 计划自检

- 规格中的认证、流式、Collector、迁移、Provider、代理池、部署和发布门禁均有对应任务。
- 任务写集已拆开；流式任务独占响应生命周期文件，设置任务独占代理配置与前端文件，避免并行写冲突。
- 未使用未决标记或“添加适当验证”等占位描述；每项都有具体测试命令、行为和提交边界。
- Migration 023 尚未在 `origin/main` 中，计划允许直接修正；若远端状态在执行中改变，则改为新增迁移而不改历史。
