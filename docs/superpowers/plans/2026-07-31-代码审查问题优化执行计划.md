# 代码审查问题优化执行计划

> **面向 Codex 的执行说明：** 必须使用 `subagent-driven-development` 技能逐任务执行。每个实现任务使用一个新的 `gpt-5.6-luna` 子代理；机械任务使用低推理强度，跨模块或并发任务使用中等强度，安全/协议/最终审查使用高强度。每个任务先写失败测试，再写最小实现，完成后运行任务级测试并由独立审查代理检查。步骤使用复选框跟踪进度。

**目标：** 修复本次全仓 code review 确认的 P1/P2 缺陷，补齐关键回归测试、前端会话一致性和部署验收门禁；P3 项作为低优先级收尾，不影响核心请求正确性。

**架构：** 保留当前 Go 单实例、SQLite、Key Pool、Vue 3 管理端和嵌入式前端架构。优先通过事务边界、上下文边界和现有 middleware/helper 修复根因，不引入 Redis、消息队列、第二套状态源或新的业务配置层。

**技术栈：** Go、SQLite、Vue 3、TypeScript strict、pnpm、Vitest、Docker Compose、Bash、GitHub Actions、Playwright。

---

## 0. 执行前基线与硬约束

**现状：**

- 工作区路径：`D:\PROJECT_ZZZZZZZZZ\nvida反代`。
- 既有工作区修改：`internal/web/dist/index.html`；不得覆盖、回滚或代替它。
- 既有未跟踪文档：`docs/superpowers/plans/2026-07-31-NVIDIA路由器第一轮收尾执行计划.md`；不得覆盖或删除它。
- 本计划文件本身也是本次新增文档；执行代码任务时不要把上述无关文件加入任务提交。
- 既有第一轮收尾计划已经覆盖 Audio 联调生命周期、Compose 初始验收和历史文档同步；本计划只处理本次 review 的新增缺陷，发现重复时以当前代码和测试证据为准，不重做已经通过的任务。

**全局规则：**

- 先读目标文件和现有测试，再编辑；不接受只改表象的 workaround。
- 每个任务使用独立子代理和不重叠写集；同一文件不得由两个实现代理并行修改。
- 测试先行：新增测试必须先单独运行并确认失败，再实现。
- Go 错误必须保留上下文；代码注释只使用英文，且只解释 why。
- 不记录或打印 NVIDIA Key、Access Key、管理员 Cookie、主密钥、请求/响应正文、SSE 内容、音频和模型输出。
- 不伪造 `capability_verified_at`，不把 `SKIP` 改写成 `PASS`，不直接修改 SQLite 绕过业务门禁。
- Windows 无 CGO/GCC、Docker、Linux Bash、Chromium 时，标记 BLOCKED 并转到 GitHub Actions；不得把静态检查冒充真实通过。

**执行前验证：**

```text
git status --short
go test -count=1 ./...
go vet ./...
pnpm --dir web run test
pnpm --dir web run typecheck
pnpm --dir web run lint
git diff --check
```

预期：当前基线命令全部通过；若基线失败，先记录失败并定位是否为既有问题，不把它混入本计划修复。

---

## 1. 任务拆分与并行关系

### 可并行任务

在基线通过后，可并行启动以下任务，写集互不重叠：

- 任务 1：NVIDIA Key 状态和 Attempt 超时。
- 任务 2：非流响应、Responses 转换和 SSE。
- 任务 3：模型事务、TOCTOU、Pool 同步和错误映射。
- 任务 4：路由鉴权、管理员会话、观测时间和音频临时目录。
- 任务 5：前端请求、session 和运行时数据校验。
- 任务 6：CLI、Live/Compose/secret scan、备份恢复文档和低优先级门禁。

### 必须串行的任务

- 任务 7 必须等待任务 1～6 完成，因为它运行全仓测试和跨模块集成验证。
- 任务 3 的 Pool 同步测试若需要修改 `internal/app/app_test.go`，必须先确认任务 4 没有同时修改该文件。
- 任务 6 的脚本和文档判定规则必须以任务 2 的 SSE/协议行为和任务 4 的路由行为为准。

每个任务完成后执行：

```text
git diff --check
git status --short
```

只检查任务写集，确认没有误改既有 `internal/web/dist/index.html` 或其他任务文件。

---

## 2. 任务 1：修复 NVIDIA Key 恢复和非流请求超时

**目标：** 成功凭证测试后恢复 Key 的调度资格；非流请求的总 deadline 覆盖状态持久化，不因数据库锁无限延长请求。

**文件：**

- 修改：`internal/nvidiakey/repository.go`
- 修改：`internal/nvidiakey/service_test.go`
- 修改：`internal/pool/pool.go`
- 修改：`internal/pool/pool_test.go`（若当前测试文件不存在，使用该包现有测试文件）
- 修改：`internal/router/attempt.go`
- 修改：`internal/router/attempt_test.go`

- [x] **步骤 1：添加 Key 恢复失败测试**

增加 `TestMarkSuccessClearsAuthInvalid`：创建或直接准备一个 `auth_invalid=1` 的 Key，调用现有 `Service.MarkSuccess`，断言数据库快照的 `AuthInvalid=false`、冷却为空、失败计数为 0。

增加 Pool 状态测试：加载同一个 `AuthInvalid=true` 快照，调用 `Pool.ApplySuccess`，再调用 `AcquireWithSnapshot`，断言该 Key 可以被调度。测试必须覆盖数据库状态和内存状态，不能只检查一个。

- [x] **步骤 2：运行失败测试**

```text
go test ./internal/nvidiakey ./internal/pool -run 'MarkSuccess|AuthInvalid|ApplySuccess' -count=1
```

预期：新增测试在实现前失败，失败原因是 `auth_invalid` 仍为 true 或 Pool 仍跳过 Key。

- [x] **步骤 3：实现恢复语义**

在 `markSuccess` 的同一条 SQL 中显式设置 `auth_invalid = 0`；在 `Pool.ApplySuccess` 中同步设置 `state.snapshot.AuthInvalid = false`。不要通过重启或重新加载快照掩盖同步缺失。

- [x] **步骤 4：添加状态持久化 deadline 测试**

增加 `TestAttemptNonstreamStatePersistenceUsesRequestDeadline`：使用 `NonstreamTotalTimeoutMS` 为几十毫秒的设置，fake `MarkSuccess`/`MarkFailure` 阻塞直到传入 context 被取消；调用 `Attempt.Run`，断言：

- `Run` 在总 deadline 的小容差内返回；
- 状态 writer 收到 context cancellation；
- lease 最终释放；
- 不会因为状态 writer 永久阻塞而继续等待。

- [x] **步骤 5：实现上下文边界**

`executeLease` 的状态写入不得再使用未带总 deadline 的原始 `ctx`。传入 `requestCtx` 或其明确有界的子 context；状态写入被 deadline 取消时，保持已有的公开错误语义，不启动无法回收的后台 goroutine。无论状态写入成功或失败，必须通过现有 defer 释放 lease。

- [x] **步骤 6：验证任务**

```text
go test ./internal/nvidiakey ./internal/pool ./internal/router -count=1
go test ./internal/router -run 'Deadline|Persistence|Lease' -count=1
git diff --check
```

---

## 3. 任务 2：收紧非流协议校验并修复 SSE 生命周期

**目标：** 上游返回结构错误的 2xx 时能被识别为协议故障并触发备用 Key；合法多行 SSE 不丢数据；超长无换行输入不会先无限分配；注释事件不能提交业务响应。

**文件：**

- 修改：`internal/upstream/nvidia/chat.go`
- 修改：`internal/upstream/nvidia/chat_test.go`
- 修改：`internal/httpapi/v1/chat.go`
- 修改：`internal/httpapi/v1/chat_test.go`
- 修改：`internal/httpapi/v1/responses.go`
- 修改：`internal/httpapi/v1/responses_test.go`
- 修改：`internal/protocol/responses/nonstream.go`（仅在错误类型需要补充时修改）
- 修改：`internal/sse/decoder.go`
- 修改：`internal/sse/decoder_test.go`
- 修改：`internal/sse/prime.go`
- 修改：`internal/sse/proxy.go`
- 修改：`internal/sse/proxy_test.go`

- [x] **步骤 1：增加非流失败测试**

在 NVIDIA Chat validator 增加以下失败 case：`{"choices":[]}`、`{"choices":[null]}`、`{"choices":"not-array"}`、缺少 `choices`。断言错误可被 `errors.Is(err, nvidia.ErrProtocol)` 识别。

在 Chat HTTP 测试中，将当前允许空 choices 的测试改为协议失败/备用 Key 断言；保留未知字段透传测试，但不能让空业务结果返回成功。

在 Responses HTTP 测试中构造首 Key 返回 2xx 但转换失败、第二 Key 返回正常响应，断言最终成功且 attempts 为 2。转换错误必须包装为 `fault.Protocol`，不能作为不可重试普通 error。

- [x] **步骤 2：运行失败测试**

```text
go test ./internal/upstream/nvidia ./internal/httpapi/v1 ./internal/protocol/responses -run 'Empty|Malformed|Protocol|Retry' -count=1
```

- [x] **步骤 3：实现最小非流校验**

要求 `choices` 为非空数组，首个 choice 必须是对象；保留 Chat 响应中未识别字段，不重新序列化无关字段。Responses 转换失败统一转为可重试的 `fault.Protocol`，但客户端请求错误仍保持不可重试。

- [x] **步骤 4：增加 SSE 回归测试**

补充以下测试：

- `chatDeltaSource` 收到一个 event 的三条 `data:` 行时，使用 `sse.JoinData` 合并后只解析一次且不丢内容；
- 超过 `MaxEventSize` 且没有换行的数据立即返回 `ErrEventTooLarge`，测试不要求读取完整 payload；
- `Prime` 先收到 `: keep-alive\n\n`、随后收到 data 时继续等待并成功回放 data；
- 只有 comment 后 EOF 时返回中断错误，不能把 Attempt 当作成功；
- `Proxy` 只有在第一个带 data 的 event 才提交 HTTP 200，comment-only event 不得提交。

- [x] **步骤 5：实现有界解码**

使用 `bufio.Reader.ReadSlice` 或等价的分段读取逻辑，在累计字节超过 `MaxEventSize` 时立即返回；不得继续使用一次性 `ReadBytes('\n')` 读取未知长度整行。保持 CRLF、EOF、注释和多行 data 的现有兼容行为。

`chatDeltaSource.Next` 必须合并同一 event 的 data 后再调用 `ParseChatDelta`；不得在循环中返回第一条后丢弃 event 的剩余 data。

- [x] **步骤 6：验证任务**

```text
go test ./internal/sse ./internal/upstream/nvidia ./internal/httpapi/v1 ./internal/protocol/responses -count=1
go test ./internal/sse -run 'Fuzz|Oversized|Multiline|Comment|Prime|Proxy' -count=1
git diff --check
```

---

## 4. 任务 3：修复模型事务一致性、并发更新和错误映射

**目标：** 模型 PATCH 不丢更新，手动验证不会验证旧版本，数据库提交后 Pool 不使用旧 block，内部错误返回 500。

**文件：**

- 修改：`internal/modelcatalog/model.go`
- 修改：`internal/modelcatalog/repository.go`
- 修改：`internal/modelcatalog/service.go`
- 修改：`internal/modelcatalog/service_test.go`
- 修改：`internal/httpapi/admin/models.go`
- 修改：`internal/httpapi/admin/models_test.go`
- 修改：`internal/pool/pool.go` 或现有同步测试文件（仅在需要暴露完整模型快照同步时）

- [x] **步骤 1：增加并发 PATCH 和 TOCTOU 测试**

增加并发 PATCH 测试：两个 goroutine 分别只修改 `display_name` 和 `enabled`，最终模型必须同时保留两个修改。

增加手测竞态测试：fake tester 在 channel 上阻塞；测试期间修改同一模型的 `upstream_id` 或 `kind`；释放 tester 后，验证操作必须返回模型版本冲突，不能写 `capability_verified_at` 或清除新版本 block。

- [x] **步骤 2：增加 Pool 同步失败测试**（架构裁定：生产 Pool setter 是无错误返回的纯内存操作，不存在可注入失败面；以 mutation result、旧 kind、新模型和同步顺序测试覆盖真实状态，不构造 fake-only error）

使用 fake model service 构造：保存前 List 失败、保存后 List 失败、旧 kind 缺失三种情况。断言失败时不会返回“数据库和 Pool 都已同步”的假成功；成功保存后 Pool 的 enabled/block 状态必须与数据库一致。

- [x] **步骤 3：实现事务内 PATCH**

新增 repository 层的事务方法，在同一个 SQLite immediate transaction 内完成：读取当前模型、应用 allowlist patch、校验并保存、读取新模型。不要继续使用 Service 外部 `Get` 加 Repository 独立 `SaveSelections` 的读改写间隔。

事务方法必须返回 `ErrModelNotFound`，并检查 `RowsAffected`；`SetEnabled`、`SetCapabilityVerified` 等更新也必须通过 `requireOneRow`，并发删除不得返回 nil。

- [x] **步骤 4：实现验证版本保护**

给内部 `Model` 增加不对外暴露的 `UpdatedAt` 或等价 revision。`VerifyAndUnblock` 接收手测开始时的 revision，事务内使用 `WHERE id = ? AND updated_at = ?` 校验；版本变化返回明确冲突错误，不能清除 block。

- [x] **步骤 5：统一数据库到 Pool 的同步结果**（事务结果直传，Save/PATCH mutation 到 Pool sync 由单实例 Handler mutex 保序；同步 setter 无 I/O/error 路径，因此不新增虚假 reload 协调器）

让 `SaveSelection`/PATCH 返回本次事务产生的旧 kind、新模型和 block 变化，Handler 直接使用这份事务结果同步 Pool，不再依赖可能失败的第二次 `List`。保存前读取失败必须立即返回；保存后无法同步时必须返回内部错误并执行一次完整 Pool reload，不能静默保留旧内存状态。

- [x] **步骤 6：修复 HTTP 错误映射**

`writeModelError` 只把 `ErrCapabilityUnverified`、`ErrManualTestRequired` 等领域错误映射为 400/404；默认分支改为既有 `writeInternalError`，避免数据库和连接错误返回 `400 invalid_request`。

- [x] **步骤 7：验证任务**

```text
go test ./internal/modelcatalog ./internal/httpapi/admin ./internal/pool -count=1
go test ./internal/modelcatalog ./internal/httpapi/admin -run 'Patch|Verify|Block|Concurrent|Error' -count=1
git diff --check
```

---

## 5. 任务 4：修复路由鉴权、会话清理、时间和临时文件边界

**目标：** 所有 `/v1/*` 路径都经过 Access Key 边界；会话不会无限增长；观测日志按真实时间排序/清理；音频使用配置的临时目录；CLI 命令响应取消。

**文件：**

- 修改：`internal/httpapi/router.go`
- 修改：`internal/httpapi/router_test.go`
- 修改：`internal/app/app.go`
- 修改：`internal/accesskey/middleware.go` 或现有 Access Key 测试（只在包装 fallback 时需要）
- 修改：`internal/adminauth/session.go`
- 修改：`internal/adminauth/repository.go`
- 修改：`internal/adminauth/session_test.go`
- 修改：`internal/app/app.go` 或新增 `internal/adminauth/cleanup.go`
- 修改：`internal/observability/repository.go`
- 修改：`internal/observability/stats.go`
- 修改：`internal/observability/observability_test.go`
- 修改：`internal/protocol/audio/transcriptions.go`
- 修改：`internal/httpapi/v1/audio.go` 及相关测试
- 修改：`internal/app/cli.go`
- 修改：`internal/app/cli_test.go`

- [x] **步骤 1：增加未知路由鉴权测试**

在 router 集成测试中请求 `/v1/unknown`、`/v1/unknown/nested`：无 Authorization 和无效 Authorization 必须返回结构化 401；有效 Access Key 才能得到 501 Unsupported。已实现的 `/v1/models` 行为不得改变。

- [x] **步骤 2：以最小写集保护 fallback**

让 `newV1Router` 接收一个已经经过 Access Key middleware 的 `unsupported` handler；在 `app.go` 中使用与已实现数据路由相同的 `DataMiddleware(accessKeys, ...)` 包装。不得给已实现 handler 再套第二层 Access Key middleware。

- [x] **步骤 3：增加并实现会话清理**

增加 `DeleteExpiredOrRevoked(ctx, cutoff)` repository 方法和对应测试，删除 `expires_at <= cutoff` 或已撤销且 `revoked_at <= cutoff` 的行。新增独立的 admin session cleanup worker，至少每日运行一次，并在 App 关闭时停止；不要把 adminauth 依赖反向塞进 observability repository。

- [x] **步骤 4：修复观测时间比较**

为 `DeleteRequestLogsBefore` 和 `ListRecentErrors` 增加带小数秒/无小数秒混合数据测试。选择一种兼容现有 SQLite 数据的实现：优先使用 SQLite 可解析的时间表达式进行比较和排序；若改为固定宽度字符串，必须提供迁移并验证旧数据。不能只改 Go formatter 而留下历史数据排序错误。

- [x] **步骤 5：传递 TempDir 和 CLI context**

把 `config.Config.TempDir` 传入音频 multipart/replay body 的构造链，保留请求结束后的清理；增加大于 1 MiB 文件位于配置目录的测试。将 `runAdminPasswordReset(context.Background(), ...)` 和 `runDatabaseBackup(context.Background(), ...)` 改为使用 `runCLIContext` 收到的 `ctx`，增加取消测试。

- [x] **步骤 6：验证任务**

```text
go test ./internal/httpapi ./internal/accesskey ./internal/adminauth ./internal/observability ./internal/protocol/audio ./internal/app -count=1
go test ./internal/httpapi ./internal/adminauth ./internal/observability ./internal/app -run 'Auth|Session|Unknown|Recent|Cleanup|Cancel|Temp' -count=1
git diff --check
```

---

## 6. 任务 5：修复前端并发请求、session 和响应结构边界

**目标：** 旧响应不能覆盖新状态；session 探测或过期后有统一恢复路径；200 响应结构错误时显示错误而不是让组件崩溃；数字输入不会提交 0/NaN。

**文件：**

- 修改：`web/src/shared/api/client.ts`
- 修改：`web/src/shared/api/client.spec.ts`
- 修改：`web/src/features/auth/useSession.ts`
- 修改：`web/src/features/auth/useSession.spec.ts`（若不存在，创建）
- 修改：`web/src/router/index.ts`
- 修改：`web/src/router/index.spec.ts`（若不存在，创建）
- 修改：`web/src/features/access-keys/AccessKeysView.vue` 及测试
- 修改：`web/src/features/nvidia-keys/NvidiaKeysView.vue` 及测试
- 修改：`web/src/features/models/ModelsView.vue` 及测试
- 修改：`web/src/features/statistics/StatisticsView.vue` 及测试
- 修改：`web/src/features/runtime/SettingsForm.vue` 及测试

- [x] **步骤 1：增加旧响应覆盖测试**

为 Access Keys、NVIDIA Keys、Models 的 `load*` 使用两个 deferred Promise：第一次请求延迟，第二次请求先返回新数据，第一次最后返回旧数据；断言页面仍显示新数据。组件卸载后响应返回时不得写入已卸载状态。

- [x] **步骤 2：实现最小请求序列保护**

每个列表组件维护递增 request sequence 或 AbortController；只有当前请求序号才允许更新数据、错误和 loading 状态。不要为一次性列表请求引入全局缓存或新状态管理库。

- [x] **步骤 3：增加 session 异常测试并实现统一失效事件**

覆盖首次 session 探测返回 500、网络异常、登录后任意管理 API 返回 401。建议在 `apiRequest` 对 401 发出内部 session-expired 事件，由 `createSessionStore` 将状态变为 anonymous；路由守卫负责回到 `/login`，并保证事件监听在测试和应用销毁时可清理。401 不应只停留在当前页面的错误文本。

- [x] **步骤 4：增加响应结构保护**

为统计、模型、Key 列表响应增加小型 type guard，至少检查顶层对象、`data` 数组和数值字段。`apiRequest<T>` 不能仅靠 TypeScript 泛型保证运行时结构。异常 200 响应转换为可见错误状态，不调用 `undefined.filter` 或 `null.toFixed`。

- [x] **步骤 5：校验运行设置输入**

提交前拒绝空值、NaN、Infinity、小数不符合字段约束和超出范围的值；保持后端错误展示。测试 `''`、`NaN`、边界值和越界值，不能让 `Number('')` 变成 0 后提交。

- [x] **步骤 6：验证任务**（生产构建使用临时 `outDir`，并以 SHA-256 证明未覆盖既有 `internal/web/dist/index.html`）

```text
pnpm --dir web run test -- --run
pnpm --dir web run typecheck
pnpm --dir web run lint
pnpm --dir web run build
```

---

## 7. 任务 6：修复测试/部署门禁和运维文档

**目标：** CI 不把缺测、未跟踪秘密、不安全 Compose 配置和错误恢复步骤当作通过。

**文件：**

- 修改：`scripts/test/live-nvidia.sh`
- 修改：`scripts/test/compose-acceptance.sh`
- 修改：`docker-compose.yml`（仅在端口/测试隔离确实需要时）
- 修改：`.github/workflows/ci.yml`
- 修改：`web/package.json`
- 修改：`scripts/check-secrets.sh`
- 修改：`.dockerignore`
- 修改：`docs/备份与恢复说明.md`
- 修改：`internal/database/migrate.go` 及测试（P3）

- [ ] **步骤 1：增加 Live 结果判定测试/静态检查**

让脚本解析 `case=<name> status=<PASS|FAIL|SKIP>`：所有必测 case 必须 PASS；可选 Audio 可 SKIP，但整体结果必须标记为未完成/非完整 PASS。脚本不得仅使用 `go test` 退出码生成 `LiveGoTest PASS`。用一个假的 `go test` 输出或可注入命令验证 PASS、SKIP、FAIL 三种结果。

- [ ] **步骤 2：补齐 Compose acceptance**

保留现有构建、health 和首次改密检查，并通过 `docker inspect` 断言运行用户为 10001、只读根文件系统、cap drop、no-new-privileges、healthcheck 和 stop grace period。增加 `docker compose down` 后重新启动并验证 named volume 数据仍在。固定端口并发执行时必须明确失败原因，或为 acceptance 提供随机宿主端口。

- [ ] **步骤 3：收紧空测试集和 secret scan**

移除 `vitest run --passWithNoTests`，让空测试集失败。secret scan 至少扫描 Docker 实际 build context 的未跟踪文件；`.dockerignore` 排除 `.env*`、数据库、日志、音频、测试产物和常见凭证文件。不要把真实秘密加入测试 fixture。

- [ ] **步骤 4：修正恢复文档**

在 `docs/备份与恢复说明.md` 的 restore 命令中明确数据库文件和所在目录必须归 UID/GID 10001，权限至少允许应用创建 WAL/SHM；恢复后执行一次写数据库的验证，而不只是 health 检查。文档必须说明错误主密钥、root-owned 文件和残留 WAL 的失败表现。

- [ ] **步骤 5：处理 future migration 和脚本语法**

让启动迁移阶段或 readiness 阶段对未知 migration 版本给出一致、可诊断的失败状态，并补充 `Migrate`/`VerifyMigrations` 的未知版本测试。Linux 环境执行 `bash -n`、secret scan、Compose acceptance；Windows 无 Bash/Docker 时只记录 BLOCKED。

- [ ] **步骤 6：验证任务**

```text
bash -n scripts/test/live-nvidia.sh
bash -n scripts/test/compose-acceptance.sh
bash scripts/check-secrets.sh
pnpm --dir web run test
git diff --check
```

Docker、真实 NVIDIA 和 Linux Bash 必须在 GitHub Actions 或受控 Linux 测试机真实执行；不能用 PowerShell 静态阅读替代。

---

## 8. 任务 7：整合验证和最终审查

- [ ] **步骤 1：运行本地可执行门禁**

```text
go test -count=1 ./...
go vet ./...
go build ./cmd/...
pnpm install --frozen-lockfile
pnpm --dir web run lint
pnpm --dir web run typecheck
pnpm --dir web run test
pnpm --dir web run build
git diff --check
```

- [ ] **步骤 2：运行 Linux/CI 门禁**

```text
go test -race ./...
go test -race ./tests/mocknvidia -count=10
golangci-lint run ./...
bash scripts/check-secrets.sh
bash scripts/test/compose-acceptance.sh
pnpm --dir web exec playwright install --with-deps chromium
bash tests/e2e/run.sh
```

每条命令记录真实退出码和关键输出；任何 BLOCKED 都保留 BLOCKED，不改成 PASS。

- [ ] **步骤 3：执行最终 Luna 高强度审查**

分派一个新的 `gpt-5.6-luna` 高推理强度审查代理，输入本计划、完整 diff、任务测试输出和未解决清单。审查范围：

- 是否仍存在数据库与 Pool 双状态源分叉；
- context cancellation 是否可能泄漏 goroutine、response body 或 lease；
- 所有 `/v1/*` 路径是否经过正确鉴权；
- SSE 正常结束、EOF、中断、comment-only、multi-data 和 oversized 行是否都有行为测试；
- 错误映射是否区分 4xx 业务错误与 5xx 内部错误；
- 测试是否能在空测试集、乱序响应和异常 200 下真正失败。

- [ ] **步骤 4：完成工作区检查**

```text
git status --short
git diff --stat
git diff --check
```

确认只包含本计划允许的代码/测试/文档改动，以及用户原有的 `internal/web/dist/index.html` 和原有未跟踪计划；不得残留临时数据库、WAL/SHM、日志、音频、Cookie 或凭证。

---

## 9. 完成标准

- P1 全部有针对性失败测试、实现和通过证据。
- P2 的 Key、Attempt、协议/SSE、模型事务、鉴权、会话、观测、临时文件和前端 session 问题全部关闭，或明确写入阻塞清单并说明原因。
- 非流 malformed 2xx、SSE comment-only、SSE multi-data、超长无换行、并发 PATCH、手测 TOCTOU、旧前端响应和恢复文件权限均有回归测试。
- Go 单元测试、race、vet、前端 test/typecheck/lint/build、Compose、secret scan 和 E2E 的结果均能追溯到实际命令。
- 真实 NVIDIA 缺少服务/凭证/模型时保持 BLOCKED；Audio 未配置时保持明确 SKIP，不得宣称完整联调通过。
- 不引入本次未请求的 Redis、PostgreSQL、Prometheus、多实例协调、HTTPS 反向代理或新的可配置业务能力。

## 10. 停止条件

- 发现任务需要覆盖用户已有修改或删除未知文件时，停止该任务并报告路径。
- 发现计划与当前真实接口、迁移或既有收尾计划冲突时，先在任务审查中列出冲突，不默默选择兼容性较差的实现。
- 真实凭证、Linux、Docker 或测试机不可用时，只停止对应验证，不伪造结果，也不为通过测试修改业务判定。
