# 测试机信息记录

- 更新日期：2026-08-01
- 用途：NVIDIA 路由器真实联调（星空代理 + NVIDIA 上游）

## 国内测试机（星空代理联调主用）

| 项 | 值 |
|---|---|
| IP | `114.55.25.190` |
| 用户 | `root` |
| 密码 | `REDACTED_CREDENTIAL` |
| SSH 端口 | 22 |
| 系统 | Ubuntu 24.04（阿里云） |
| 出口 IP | `114.55.25.190`（国内） |
| Docker | 29.1.5 |
| 内存 | 1.6 GB |
| 项目目录 | `/opt/nvidia-router` |
| 应用端口 | 3756（127.0.0.1） |
| 容器名 | `nvidia-router-smoke` |
| 数据卷 | `nvr-data` |

说明：
- **必须使用国内出口**调用星空提取 API——星空代理仅支持国内访问，海外 IP（如 `149.71.241.250`）会返回 `not within the scope of service`。
- 星空 API 对访问来源 IP **自动加白**（订单开启"白名单自动添加"），无需手动配置白名单。
- 容器内 fetcher 直连星空 API（不走代理），CONNECT 隧道经提取的代理 IP 转发 NVIDIA HTTPS。

## 海外测试机（其他用途，星空代理不可用）

| 项 | 值 |
|---|---|
| IP | `149.71.241.250` |
| 用户 | `root` |
| 密码 | `REDACTED_CREDENTIAL` |
| SSH 端口 | 22 |
| 说明 | 出口为海外 IP，星空代理拒绝其访问提取 API；可用于无需国内出口的测试 |

## 安全注意

- 本文件包含测试机 root 密码，属敏感信息；不得提交到公开仓库（本仓库为私有）。
- 测试机上的临时脚本、mock 进程和测试数据在联调完成后清理。
