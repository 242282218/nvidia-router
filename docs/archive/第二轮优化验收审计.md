# 第二轮优化验收审计

更新日期：2026-08-09

## 结论

代码实现、Go/前端自动化验证和本机 Git Bash Playwright E2E 已完成；真实国内测试机联调已通过，代理链路、凭据和业务混合负载均已验证。

## 逐项证据

| 项目 | 状态 | 证据 |
|---|---|---|
| R1.1 settings 契约 | 通过 | `internal/httpapi/admin/settings.go`、`settings_test.go`、`tests/e2e/runtime.spec.ts` |
| R1.2 轮转活动写者检测 | 通过（单测） | `internal/app/cli.go` 的 `.db-shm` 检测；`TestCLIRotateMasterKey*` |
| R1.2 容器启动后/停服后命令 | 通过（真实远程验收） | 使用包含最新 `rotate-master-key` 的临时镜像：服务运行时退出 1 并输出 `detected an active writer; master key rotation must run with the service stopped`；停服后退出 0 并输出 `Master key rotation completed`。证据：`artifacts/r1-2-rotation-20260809.txt`。验收后已恢复原数据库并重启服务。 |
| R1.3 Secure Cookie/loopback/TLS 警告 | 通过 | `internal/config/config.go`、`docker-compose.yml`、`README.md` |
| R2.1 流式槽位 | 通过（单测） | `internal/pool/streaming_test.go`、`internal/router/stream_quota_test.go`、migration 011 |
| R2.2 重试收紧 | 通过（单测） | `internal/fault/classifier_test.go`、`internal/upstream/nvidia/proxy_client_test.go` |
| R2.3 Retry-After | 通过（单测） | `internal/router/retrybudget_test.go` |
| R2.4 分批删除 | 通过（单测） | `internal/observability/batched_delete_test.go`；每批日志包含 `batch`、`deleted`、`duration_ms`、`total_deleted` |
| R2 真实代理联调 | 通过（真实环境） | `114.55.25.190`：代理池健康返回 `ok`，认证代理到 `https://example.com` 的 CONNECT 探针返回 HTTP 200；同步当前模型 `minimaxai/minimax-m3` 后，单请求 HTTP 200，3 个并发流式请求 3/3 HTTP 200，10 个并发短请求 10/10 HTTP 200。证据：`artifacts/domestic-proxy-healthz-20260809.txt`、`artifacts/domestic-success-load-20260809.txt`、`artifacts/key-file-live-evidence-20260809.txt`。 |
| R3.1 Admin 契约快照 | 通过 | `internal/httpapi/admin/contract_test.go`、`internal/httpapi/admin/testdata/contract_*.json` |
| R3.2 Access Key 策略 UI | 通过（前端测试） | `AccessKeysView.vue`、`EditAccessKeyPolicyDialog.vue`、`AccessKeysView.spec.ts` |
| R3.3 dead code/死表 | 通过 | statistics API/types 已清理；migration 012；migration 测试通过 |
| R4.1 Provider 抽象 | 通过（编译/测试） | `internal/provider/provider.go`、`internal/upstream/nvidia/provider.go` |
| R4.2 TTFT 记录与 P50/P95 | 通过（单测/前端） | migration 013、observability/monitoring 测试、`StatisticsView.vue` |
| R4.3 文档与备份脚本 | 通过（脚本语法） | 四个部署/安全文档；`scripts/backup-cron-example.sh`；Git Bash `bash -n` 返回 0 |
| 完整 E2E | 通过 | Git Bash 缺少 `setsid`，已让 `tests/e2e/run.sh` 在无 `setsid` 时直接启动子进程；7/7 Playwright 用例通过 |

## 自动化回归结果

- `go test ./... -count=1`：通过。
- `go vet ./...`：通过。
- `pnpm --dir web run test`：129/129 通过。
- `pnpm --dir web run typecheck`：通过。
- `pnpm --dir web run lint`：通过。
- `go test ./internal/httpapi/admin -run TestAdminAPIContractSnapshots`：通过。
- `go test ./internal/app -run TestCLIRotateMasterKey`：通过。
- `go test ./internal/observability -count=1`：通过。
- `git diff --check`：通过。

## 环境阻塞项

1. 已获得管理员临时 SSH 入口并登录国内测试机；代理池健康、代理认证和 CONNECT 探针均通过，当前模型同步后单请求及 3 流式 + 10 短请求业务混合负载全部 HTTP 200。
2. 代理池 `curl -fsS http://127.0.0.1:8080/healthz` 已在代理池网络命名空间内执行并返回 `ok`；当前执行环境仍不能将公网端口探测作为本地健康证据。
3. Windows Git Bash 没有 `setsid`；已在 `tests/e2e/run.sh` 增加直接子进程回退，完整 E2E 已在 Git Bash 执行通过。
4. `bash -n scripts/backup-cron-example.sh` 已在 Git Bash 单独执行并返回 0。
5. 真实混合负载已完成：冷却后 3 个并发流式请求和 10 个并发短请求全部 HTTP 200；此前 410 根因是已过期的 `deepseek-ai/deepseek-v4-flash` 模型映射，已同步当前 `minimaxai/minimax-m3` 候选。
6. R1.2 真实容器门禁已完成：通过临时镜像注入当前 Linux 二进制执行了启动中拒绝和停服后成功两条路径；测试数据库已恢复，服务已重启。

真实业务联调已完成；如更换模型或凭据，按以下命令复核：

```bash
# 国内机：确认代理池健康、认证和 CONNECT 链路，再跑 3 流式 + 10 短请求
curl -fsS http://127.0.0.1:8080/healthz

# Linux/Docker 环境
bash -n scripts/backup-cron-example.sh
tests/e2e/run.sh
```
