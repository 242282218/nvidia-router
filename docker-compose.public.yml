# 公网监听覆盖。内置 XApi 采集器与代理池随 app 单体运行。
# 用法：docker compose -f docker-compose.yml -f docker-compose.public.yml up -d --build
services:
  app:
    ports:
      - "0.0.0.0:3756:3756"
    environment:
      NVIDIA_ROUTER_LISTEN_ADDR: "${NVIDIA_ROUTER_LISTEN_ADDR:-0.0.0.0:3756}"
